{{ form_start(form, {'attr': {'class': 'mb-4'}}) }}
    <div class="form-group">
        {{ form_widget(form) }}
    </div>

    <div class="mt-3">
        <button type="submit" class="btn bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-pill fw-bold shadow-sm">
            {{ button_label|default('Enregistrer') }}
        </button>
    </div>
{{ form_end(form) }}
